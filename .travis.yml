sudo: required
services:
  - docker

before_install :
  - docker build -t nabeegacryptocode/react-test -f ./client/Dockerfile.dev ./client

scripts:
  - docker run -e CI=true nabeegacryptocode/react-test npm test


after_success:
  - docker build -t nabeegacryptocode/multi-client ./client
  - docker build -t nabeegacryptocode/multi-nginx  ./nginx
  - docker build -t nabeegacryptocode/multi-server ./server
  - docker build -t nabeegacryptocode/multi-worker ./worker

  #Log in to Docker cli
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  #Take images and push to docker hub
  - docker push nabeegacryptocode/multi-client 
  - docker push nabeegacryptocode/multi-nginx    
  - docker push nabeegacryptocode/multi-server 
  - docker push nabeegacryptocode/multi-worker 

